//! API interface counterpart for `core/authz_interceptor`.
//!
//! The concrete implementation lives in `core/authz_interceptor`.
//! Struct and constructor definitions live in `api/authz/authz_interceptor.rs`.

pub(crate) mod impls;
