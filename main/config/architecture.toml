# SEA project configuration (L0) — swe-edge ingress aggregator
[project]
name = "swe-edge-ingress"
workspaces = [
    "grpc",
    "http",
    "message-broker",
    "tls",
    "verifier",
]

[workspace.grpc]
path = "grpc/main/features/*"

[workspace.http]
path = "http/main/features/*"

[workspace."message-broker"]
path = "message-broker/main/features/*"

[workspace.tls]
path = "tls"
crate = "swe-edge-ingress-tls"

[workspace.verifier]
path = "verifier"
crate = "swe-edge-ingress-verifier"


