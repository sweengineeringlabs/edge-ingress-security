# SEA project configuration (L0)
[project]
name       = "swe-edge-ingress"
workspaces = ["gateway", "http", "http/transport", "grpc", "grpc/transport", "grpc/reflection", "tls"]

[whitelist]
allow = ["main"]
