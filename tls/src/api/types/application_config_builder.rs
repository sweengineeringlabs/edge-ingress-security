//! `ApplicationConfigBuilder` — maps to `config/application.toml`.

/// Config builder type corresponding to `config/application.toml`.
pub type ApplicationConfigBuilder = swe_edge_configbuilder::ConfigBuilderImpl;
